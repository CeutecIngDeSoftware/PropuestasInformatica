<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
            <!-- left column -->
            <div class="col-md-6">	
		<div class="box box-info">
                <div class="box-header">
                  <h3 class="title-box">Nuevo Estado</h3>
                </div>	
	
  	 <div class="box-body">
	  	<div class="input-group">
                    <span class="input-group-addon"><i class="fa fa-check-square-o"></i></span>
		   <%= f.text_field :name, class:"form-control",:placeholder => "Ingrese Estado" %>  		  
	 	</div>
	      </div>
	  <div class="box-footer">
	  <p><%= f.submit 'Crear Estado', class: "btn btn-primary"  %></p>
	  </div>
<% end %>


</section>
</div>
