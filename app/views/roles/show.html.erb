<% if current_user %>
<div class="content-wrapper">
 <section class="content-header">

<% if userIsAdmin %>
<p id="notice"><%= notice %></p>
	<div class="row">
            <!-- left column -->
            <div class="col-md-6">	
		<div class="box box-info">
                <div class="box-header">
                  <h3 class="title-box">Informacion del Rol</h3>
                </div>

	<div class="box-body">
	<div class="input-group">
                    <span class="input-group-addon"><i class="fa fa-sitemap"></i></span>
		    <input class="form-control" placeholder="<%= @role.name %>" disabled="" type="text">
	 	</div>
	<br>


<%= link_to 'Editar', edit_role_path(@role) %> |
<%= link_to 'Regresar', roles_path %>
<% else %>
	<%= controller.redirect_to requests_path %>
<% end %>

</section>
</div>
<% else %>
<%= controller.redirect_to log_in_path %>
<% end %>
