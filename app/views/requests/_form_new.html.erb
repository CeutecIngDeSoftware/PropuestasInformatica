<%= form_for(@request) do |f| %>
  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
      <% @request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
            <!-- left column -->
            <div class="col-md-6">	
		<div class="box box-info">
                <div class="box-header">
                  <h3 class="title-box">Nueva Propuesta</h3>
                </div>	

  <div class="box-body">
	<div class="input-group">
	    <%= f.label :Clase %><br>
	    <select name="request[course_id]", class="form-control">
	      <% @courses.each do |c| %>
		<option value="<%= c.id %>"><%= c.name %> — Req: <%=c.requirements%></option>
	      <% end %>
	    </select>
  	</div>
	    <%= f.label :Horario %><br>
	    <select name="request[schedule_id]", class="form-control">
	      <% Schedule.all.each do |s| %>
		<option value="<%= s.id %>"><%= s.name %></option>
	      <% end %>
	    </select>
  	</div>
</div>
  <div class="actions">
<input type= "submit" value="Crear Propuesta" class="btn btn-primary" Submit onClick="alert('Recuerde que entre más alumnos registren la clase tienes mas probabilidades de que abran la sección.');"></a>
  </div>
<% end %>

<p><%= notice %></p>
</section>
</div>
